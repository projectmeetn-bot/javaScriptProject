/*
Create an array:

const colors = ["Red", "Green", "Blue", "Yellow"];

Using only forEach(), print
*/

const colors = ["Red", "Green", "Blue", "Yellow"];

colors.forEach((color) => {
   console.log(color) 
}) 