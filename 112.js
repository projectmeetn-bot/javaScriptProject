/*
Write a Node.js program that:

Uses a try block and a catch block.
Inside the try block, print an undefined variable.
Inside the catch block, print:
Something went wrong!
*/

try {
    console.log(name)
} catch (error) {
    console.log('Something went wrong!')
}