/*
Write a program that prints the index and color.

const colors = ["Red", "Green", "Blue", "Yellow"];
*/

const colors = ["Red", "Green", "Blue", "Yellow"];

colors.forEach((color , index) => {
    console.log(index , color)
})